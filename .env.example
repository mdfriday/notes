# API Configuration
# Set to 'true' to use the real API instead of mock data
VITE_USE_REAL_API=false

# Advanced API Configuration
# Override base API URL if needed (default uses environment-specific values)
# VITE_API_BASE_URL=http://custom-api-server:8080 